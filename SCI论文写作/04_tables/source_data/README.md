# Table Source Data

Store machine-readable source data for manuscript tables here.

Expected Paper 1 outputs:

- `table1_database_characteristics_YYYYMMDD.csv`
- `table2_variable_availability_YYYYMMDD.csv`
- `table3_field_audit_summary_YYYYMMDD.csv`

Exact manuscript numbers must be read from these files after the freeze gate is accepted.

