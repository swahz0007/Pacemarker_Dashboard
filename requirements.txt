# Runtime dependencies for the backend extraction and reporting workflow.
# Exact versions keep local and CI runs reproducible.
openpyxl==3.1.5
xlrd==2.0.2
pandas==2.3.3
matplotlib==3.10.8
seaborn==0.13.2
